let value;

const value1 = 10;
const value2 = 4;

//ARITHMETIC OPERATOR

value = value1 + value2;
value = value1 - value2;
value = value1 * value2;
value = value1 / value2;

value = Math.PI;
value = Math.E;

value = Math.round(3.6);//sayaı yuvarlama

value = Math.ceil(3.2);// yukarı yuvarlama

value = Math.floor(3.2); // aşağı yuvarlama

value = Math.sqrt(16); //karekök

value = Math.abs(-10); //mutlak değer

value = Math.pow(8,3); //power kuvveti

value = Math.max(10,-1,100,32);//max değer

value = Math.min(1,24,78,-7);// min değer

value = Math.random(); // random sayı uretir

value = Math.floor(Math.random() * 20); // 0 ve 20 arası tam sayı(aşağı yuvarlayarak) değerler



console.log(value);